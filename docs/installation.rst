============
Installation
============

At the command line::

    $ easy_install quartet_capture

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv quartet_capture
    $ pip install quartet_capture
